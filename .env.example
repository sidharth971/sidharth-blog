VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Used only by scripts/publish-post.mjs (Node, never shipped to the browser
# bundle since these aren't VITE_-prefixed). This is the owner's own login —
# the same account used on /login — not a service-role key.
SUPABASE_OWNER_EMAIL=
SUPABASE_OWNER_PASSWORD=
